sudo: required

language: java
jdk:
- oraclejdk8
before_install:
- pip install --user codecov
install: mvn install -DskipTests=true -Dmaven.javadoc.skip=true

after_success:
- codecov

branches:
  only:
  - source-analysis
services:
- mysql

script:
- mvn clean org.jacoco:jacoco-maven-plugin:prepare-agent package sonar:sonar -Dsonar.projectKey=Devonmusa_qmq -Dsonar.organization=devonmusa-github -Dsonar.branch.name=source-analysis -Dsonar.host.url=https://sonarcloud.io   -Dsonar.login=31bc04fb697701835e117c1dc00e4989a06bf3fb
